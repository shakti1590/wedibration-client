import React from 'react';

const UserDashboard = () => {
  return (
    <div>
      <h1>user Access</h1>
      <p>Only role == "user" can view this page.</p>
    </div>
  );
};

export default UserDashboard;
